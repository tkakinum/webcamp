<body>
  <div class="container">
    <div class="row">
      <div class="col-lg-4">
        <h2>User info</h2>
        <img class="attachment user profile_image fallback" src="/assets/no_image.jpg" alt="No image">
        <table class="table">
          <tbody>
            <tr></tr>
          <tr>
            <th>name</th>
            <th><%= @user.name %></th>
          </tr>
          <tr>
            <th>introduction</th>
            <th><%= @user.introduction %></th>
          </tr>
            </tbody>
        </table>
        <div class="row">
          <div>
            <%= link_to "", new_user_path(current_user.id),class: "col-xs-8 btn btn-default glyphicon glyphicon-wrench" %>
          </div>
        </div>

        <h2>New book</h2>
            <%= form_for(@book) do |f| %>
          <h4>Title</h4>
            <%= f.text_field :title %>
          <h4>Opinion</h4>
            <%= f.text_field :body %>
            <%= f.submit 'Create Book' %>
          <% end %>

          <p id="notice"></p>

      </div>

      <div class="col-lg-8">
      	<h2>Users</h2>
      	<table>
      		<thead>
        		<tr>
      	      <th>image</th>
      	      <th>name</th>
      	      <th colspan="3"></th>
      	    </tr>
      		</thead>

        	<tbody>
            <% @users.each do |user| %>
              <tr>
              <td>
                <img class="attachment user profile_image fallback" src="/assets/no_image.jpg" alt="No image" style: width: 100px; height: 100px;>
              </td>
              <td>
                <%= user.name %>
              </td>
              <td>
                <%= user.introduction %>
              </td>
              <td>
                <%= link_to "Show", user_path(user.id) %>
              </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</body>