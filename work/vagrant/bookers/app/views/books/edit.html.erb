<body>
    <h1>Editing Book</h1>

      <%= form_for(@blog) do |f| %>
        <h4>Title</h4>
        <%= f.text_field :title %>
        <h4>Body</h4>
        <%= f.text_field :body %>
        <%= f.submit 'Update Book' %>
      <% end %>

<%= link_to "Show", show_path(show) %>
<a class="back" href="/books">Back</a>

</body>